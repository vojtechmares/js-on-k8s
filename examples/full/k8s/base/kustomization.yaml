apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - pdb.yaml
  - ingress.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: app
    literals:
      - LOG_LEVEL=info
      - GREETING=hello
