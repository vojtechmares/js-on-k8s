# Safe to commit: the token comes from the environment at install time.
# Point your private scope at its registry; the public registry stays default.
@acme:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
